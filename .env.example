# Discord設定
DISCORD_TOKEN=your_discord_token_here

# Google Gemini API設定（Geminiを使用する場合）
GEMINI_API_KEY=your_gemini_api_key_here

# LM Studio API設定（LM Studioを使用する場合）
# 通常は設定不要です。LM Studio をホストで実行し、ボットも同じ環境で動かす場合は
# デフォルトの http://localhost:1234/v1 が自動的に使用されます。
# コンテナからホストの LM Studio へ接続するなど特別な URL が必要なときのみ指定してください。
#   例: Windows/Macでは http://host.docker.internal:1234/v1
#       Linuxでは http://172.17.0.1:1234/v1
# API呼び出し時に /chat/completions が自動で付与されるため、ここでは /v1 までを指定します
# LMSTUDIO_API_URL=http://lmstudio-api:1234/v1

# （必要に応じて）LM Studio APIキー
# LMSTUDIO_API_KEY=lm-studio
